allow shell device:dir { open read };
allow shell debugfs:dir read;
allow shell sysfs:file { open read write getattr };
allow shell system_prop:property_service set;
allow shell system_data_file:sock_file { getattr write };
allow shell usbmuxd:unix_stream_socket connectto;
